# Topic 09: A minimum docker + mysql compose file

## Overview
This sample project demonstrates how to set up a minimal Docker Compose environment featuring a MySQL database along with Adminer (a lightweight database management tool).

## Purpose
To show how to quickly spin up a MySQL database using Docker, populate it with an initial script, and access it through a web-based interface.
