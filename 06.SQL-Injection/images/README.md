# Images

Place screenshots and short screencasts of the SQL injection demos here
(for example: UNION password dump, error-based leak, boolean-blind response
difference, and the secure form rejecting the same payload).
